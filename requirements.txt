# Watch Tower — Python pip dependencies (the only required non-stdlib imports in the project).
# Install torch from the CUDA index that matches your driver (see the recreate guide).
# Everything else the app uses (json, sqlite3, subprocess, urllib, shutil, ...) is stdlib.
torch
gradio
pandas
sqlite-vec   # vector store for rag.py (see RECREATE-WINDOWS.md / RECREATE-LINUX.md §12)

# Linux ONLY — the Linux collectors use psutil instead of PowerShell/CIM. Uncomment on Linux:
# psutil

# OPTIONAL deep-sensor deps — every collector degrades gracefully without them:
# liquidctl       # AIO liquid temp / pump RPM fallback when LHM is down (sensors.py)
# openrgb-python  # RGB zone state via the OpenRGB SDK server (lights.py)
# SoapySDR is NOT pip-installable on Windows — it ships with radioconda/PothosSDR (sdr/rx/tx/tuner.py)
